platform :ios, '11.0'

target 'Hackathon' do
  use_frameworks!
    pod 'GooglePlaces'
    pod 'GooglePlacePicker'
    pod 'GoogleMaps'
    pod 'Firebase/Core'
    pod 'Firebase/Firestore'
    pod 'Firebase/Storage'
    pod 'FirebaseUI'
    pod 'SDWebImage', '~> 4.0'
end
## 'FirebaseUI' installs all UI components vs. pod 'FirebaseUI/Auth' and pod 'FirebaseUI/Google'. It's more than we need at this point, but it makes the app ready for extension later on and the overhead isn't very significant.
